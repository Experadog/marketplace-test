🛍️ marketplace-test

Прототип маркетплейса на React + Vite без реального backend.
Данные берутся из локального JSON-файла src/data/products.json.

Главная страница показывает список товаров с бесконечной прокруткой;
при клике по карточке открывается детальная страница с атрибутами и списком предложений продавцов.
Сортировка предложений работает по цене (по умолчанию, по возрастанию) и по дате доставки (по возрастанию).

⸻

⚙️ Требования
	•	Node.js 18+ (рекомендовано 18 или 20; проверено на 24)
	•	npm (или pnpm / yarn — по желанию)

⸻

🚀 Установка

# 1. Клонируем репозиторий
git clone <URL_ВАШЕГО_РЕПО> marketplace-test
cd marketplace-test

# 2. Устанавливаем зависимости
npm install

▶️ Запуск (быстрый старт)

Сразу после клонирования в проекте есть минимальный
src/data/products.json с одной карточкой, чтобы вы увидели верстку и логику без генерации.

# 3. Запускаем дев-сервер
npm run dev

Откройте в браузере 👉 http://localhost:5173
	•	На главной будет одна карточка (это нормально).
	•	Бесконечный скролл включится, когда данных станет больше.

⸻

🧩 Генерация данных (≈ 120 товаров)

В проекте есть генератор, создающий ~120 товаров с уникальными картинками,
ценами в USD / EUR / RUB, остатком, рейтингом и датой доставки в пределах текущей недели (Пн–Вс).
Также генерируются атрибуты и список офферов для детальной страницы.

# 4. Сгенерировать новые данные
npm run generate

После выполнения скрипта будет перезаписан src/data/products.json.
Обновите страницу — на главной появится список товаров с бесконечной подгрузкой.

Где лежит генератор: src/scripts/generateProducts.mjs
Куда пишет данные: src/data/products.json

⚙️ Настройки генерации

Откройте src/scripts/generateProducts.mjs и при необходимости измените:

Константа => Назначение
COUNT Количество => генерируемых товаров (по умолчанию 120)
CURRENCIES Список => валют (["USD", "EUR", "RUB"])
getThisWeekRange() => Логика расчёта текущей недели (Пн–Вс)
makeAttributes() и SELLERS => Схема атрибутов и список продавцов

🧭 Навигация по приложению

Страница   Путь          Описание
Главная    /             Список товаров с бесконечной прокруткой
Детальная  /product/:id  Карточка товара с атрибутами и предложениями продавцов

Пример: http://localhost:5173/product/p-0001 (ID берите из src/data/products.json)

🔄 Сортировка офферов
	•	По умолчанию: по цене (возрастание)
	•	«По цене» — сортирует по цене
	•	«По дате доставки» — сортирует по дате (раньше → раньше)

При повторном клике по активной кнопке порядок меняется (↑ / ↓).
Текущие настройки отображаются стрелками в кнопках.

🧱 Сборка и предпросмотр production-версии

# Сборка проекта
npm run build

# Локальный предпросмотр собранного бандла
npm run preview

🗂️ Структура проекта (упрощённо)

src/
├─ data/
│  └─ products.json          # Основной источник данных (генерируется скриптом)
├─ scripts/
│  └─ generateProducts.mjs   # Генератор мок-данных (~120 товаров)
├─ components/
│  ├─ ProductCard.jsx
│  ├─ ProductGrid.jsx
│  ├─ ProductDetails.jsx
│  └─ InfiniteScroll.jsx
├─ pages/
│  ├─ HomePage.jsx           # Главная страница
│  └─ ProductPage.jsx        # Детальная страница товара
├─ utils/
│  ├─ formatDate.js
│  └─ formatPrice.js
├─ App.jsx
├─ main.jsx
└─ index.css                 # Стили (включая выравнивание 3 блоков по 600 px)

💬 Частые вопросы

После npm run dev вижу только 1 карточку.
Это нормально — в src/data/products.json по умолчанию 1 товар.
Запустите npm run generate, чтобы создать ~120 товаров.

Генератор создал данные в неправильную папку.
Убедитесь, что в src/scripts/generateProducts.mjs указан правильный путь:

const OUT_PATH = "../data/products.json";

Картинки не грузятся.
Изображения берутся с https://picsum.photos по стабильному seed — нужен интернет.

Хочу изменить валюту / диапазон цен / дату доставки.
Правьте соответствующие константы в src/scripts/generateProducts.mjs
(CURRENCIES, basePrice, randomDateThisWeekISO()).

📞 Контакты

Если что-то пойдёт не так — пишите:
https://t.me/experadog

